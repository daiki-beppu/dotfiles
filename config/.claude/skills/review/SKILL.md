---
name: review
description: |
  変更コードの品質チェックを一気通貫で実行するスキル。
  simplify → security-review → CLAUDE.md 同期の順で実行し、結果をサマリー表示する。
  「レビューして」「品質チェック」「セルフレビュー」「/review」で発動。
  WIP 確認や PR を出さないセルフレビューなど、品質チェックだけ単独で走らせたい場面で使う。
  注意: コミット・プッシュ・PR 作成は行わない。修正はワーキングツリーに残す。
---

# review — 品質チェックパイプライン

## Overview

変更コードに対して品質チェックを順番に実行するオーケストレータースキル。
各ステップは既存スキルに委譲し、問題があれば自動修正して報告する。
コミットやプッシュは行わず、修正はワーキングツリーに残す。

## When to Use

- コードの品質チェックだけ実行したいとき
- WIP の状態で問題がないか確認したいとき
- PR を出す前のセルフレビューとして
- `/review` コマンドを実行したとき
- 「レビューして」「品質チェック」と言われたとき

## パイプライン

### Step 0: Context 収集

以下のコマンドで現在の状態を確認：

```bash
git branch --show-current           # 現在のブランチ
git status --short                  # ステータス
git diff --stat main..HEAD          # 変更ファイルの概要
git log main..HEAD --oneline        # main からの差分コミット
```

**ゲート**: 現在のブランチが `main` または `master` の場合はここで停止し、feature ブランチの作成を促す。

### Step 1: コード簡素化レビュー

`/skills simplify` を Skill ツールで呼び出す。

変更されたコードの品質・可読性・再利用性をレビューし、問題があれば自動修正する。
修正内容をユーザーに報告して次のステップへ進む。

### Step 2: セキュリティレビュー

`/skills security-review` を Skill ツールで呼び出す。

差分に対してセキュリティ観点のレビューを実施する。

- **CRITICAL**（シークレット漏洩、インジェクション等）: 修正必須。修正してユーザーに報告
- **WARNING 以下**: 自動修正可能なら修正、不可能なら報告のみ

### Step 3: CLAUDE.md 同期

**条件**: 以下のコマンドで CLAUDE.md 関連の変更があるか確認する。

```bash
git diff main..HEAD --name-only | grep -i "claude.md"
```

変更がある場合のみ `claude-md-management:claude-md-improver` を Skill ツールで呼び出す。
変更がない場合はスキップして次のステップへ。

### 完了: サマリー表示

全ステップの結果をまとめて表示する：

- 各ステップで発見された問題と修正内容
- 未修正の警告事項（あれば）
- ワーキングツリーの状態（`git status --short`）

## エラーハンドリング

| 状況 | 対応 |
|------|------|
| main ブランチ上 | エラー終了、ブランチ作成を促す |
| main との差分なし | 警告表示、変更がないことを報告 |

## Rules

- 各ステップは必ず順番に実行する（並列実行しない）
- 問題を発見したら自動修正し、修正内容をユーザーに報告する
- コミット・プッシュ・PR 作成は絶対に行わない
- 修正はワーキングツリーに残し、ユーザーが確認できるようにする
