---
name: issue
description: |
  GitHub issue を会話コンテキストから自動生成して `gh issue create` で作成する。
  ユーザーが issue 作成を依頼したら必ずこのスキルを使うこと —
  「issue にして」「issue 作って」「これ issue に」「バグ報告して」「機能リクエスト出して」
  「TODO を issue に」「issue 立てて」「issue にまとめて」「追跡用に issue」など、
  issue の新規作成に関わるあらゆる表現で発動する。
  ユーザーが問題点・バグ・改善案・タスクに言及し、それを GitHub issue 化するよう
  求めている場面では、たとえ「issue」という単語がなくても（例:「これバグ報告しといて」
  「追跡用にチケット切って」）このスキルを使うこと。/issue コマンドでも起動する。
  注意: issue の閲覧・検索・クローズ・コメント追加は対象外。新規作成のみ。
---

# issue

## Overview

会話コンテキストから GitHub issue を自動生成するスキル。会話の流れから適切なタイトル・本文・ラベルを推測し、ユーザー確認後に `gh issue create` で作成する。

## When to Use

- ユーザーが「issue にして」「issue 作って」「これ issue に」と言ったとき
- `/issue` コマンドを実行したとき
- バグ報告、機能リクエスト、TODO の issue 化を依頼されたとき

## Context を収集

以下のコマンドで現在の状態を確認：

```bash
gh repo view --json nameWithOwner   # リポジトリ名
git branch --show-current           # 現在のブランチ
gh label list --json name,description  # 既存ラベル一覧
```

## Task

1. **コンテキスト収集**: 上記コマンドを実行し、リポジトリ情報と既存ラベルを取得する
2. **issue 内容を生成**: 会話の文脈から以下を生成する
   - **タイトル**: 簡潔で具体的な日本語タイトル（50 文字以内目安）
   - **本文**: 背景・現状・期待する動作を構造化して記述
   - **ラベル**: 既存ラベル一覧から適切なものを選択（なければラベルなし）
3. **プレビュー表示**: 生成した内容をユーザーに提示する。以下のフォーマットで表示：

   ```
   ## Issue プレビュー

   **リポジトリ**: owner/repo
   **タイトル**: ここにタイトル
   **ラベル**: bug, enhancement（該当するもの）

   **本文**:
   ここに本文
   ```

4. **ユーザー確認**: 「この内容で作成しますか？修正があれば教えてください」と確認する。修正指示があれば反映してから再度プレビューする
5. **issue 作成**: 承認後、`gh issue create` で作成する

   ```bash
   gh issue create --title "タイトル" --body "本文" --label "label1,label2"
   ```

6. **URL を表示**: 作成された issue の URL を表示する

## Rules

- ラベルは必ず既存ラベル一覧から選択する。存在しないラベルを勝手に作成しない
- ユーザーの承認なしに `gh issue create` を実行しない
- 本文は日本語で書く（技術用語・コード・識別子はそのまま）
- 会話中のコードスニペットやエラーメッセージは本文にコードブロックとして含める
- 引数でタイトルや内容が直接指定された場合は、それを優先する
- 本文テンプレートは固定せず、内容に応じて最適な構造を使う（バグならば再現手順、機能リクエストならばモチベーションと提案など）
